//Size-Of-Function
#include<stdio.h>
#include<conio.h>
void main()
{
    int a = 5;
    float b = 6.5;
    printf("\n Size of the integer = %d bytes",sizeof(int));
    printf("\n Size of the float = %d bytes",sizeof(float));
    printf("\n Size of the variable a = %d bytes",sizeof(a));
    printf("\n Size of the variable b = %d bytes",sizeof(b));
}
